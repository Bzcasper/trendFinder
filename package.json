{
  "name": "trend-finder",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "copy-files": "cpy src/**/*.py dist --parents",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "cpy-cli": "^3.1.1"
  }
}
